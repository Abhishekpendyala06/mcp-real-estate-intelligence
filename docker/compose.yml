version: '3.8'
services:
  app:
    build: ..
    environment:
      NODE_ENV: development
      REDIS_URL: redis://redis:6379
    env_file: ../.env
    ports:
      - "3000:3000"
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
